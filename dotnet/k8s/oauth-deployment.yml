apiVersion: apps/v1
kind: Deployment
metadata:
  name: bt-dotnet-oauth-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bt-dotnet-oauth-demo
  template:
    metadata:
      labels:
        app: bt-dotnet-oauth-demo
    spec:
      containers:
        - name: bt-dotnet-oauth-demo
          image: ghcr.io/example/bt-dotnet-demo:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: bt-dotnet-oauth-config
            - secretRef:
                name: bt-dotnet-oauth-secret
