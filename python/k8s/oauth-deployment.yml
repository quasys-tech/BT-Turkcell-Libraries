apiVersion: apps/v1
kind: Deployment
metadata:
  name: bt-python-demo-oauth
  namespace: bt-turkcell
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bt-python-demo-oauth
  template:
    metadata:
      labels:
        app: bt-python-demo-oauth
    spec:
      containers:
        - name: bt-python-demo-oauth
          image: docker.io/enespekdas/btpython-lib:v1.0
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: bt-python-demo-oauth-secret
